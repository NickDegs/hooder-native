import SwiftUI

// ── Hooder — NATIVE (iOS 26 / Swift 6, Liquid Glass) ──────────────────────────
// Gerçek 3D uydu harita (offline), canlı mülk pin'leri, Liquid Glass + mercek
// yanması + ultra yumuşak spring animasyonlar. Konum izni İSTENMEZ.
@main
struct HooderApp: App {
    var body: some Scene {
        WindowGroup {
            RootView()
        }
    }
}
